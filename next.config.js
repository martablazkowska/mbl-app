/** @type {import('next').NextConfig} */
const nextConfig = {
  compiler: {
    styledComponents: true,
  },
  experimental: {
    // appDir: true,
  },
  reactStrictMode: true,
};

module.exports = nextConfig;
